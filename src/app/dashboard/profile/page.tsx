"use client";
import AccountProfilePanel from '@/components/AccountProfilePanel';

export default function ProfilePage() {
  return <AccountProfilePanel title="Account" description="This is the same account section used inside Settings." compact />;
}
